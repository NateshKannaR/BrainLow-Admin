services:
  - type: web
    name: brainlow-admin
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT admin_app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
    envVarGroups:
      - NateshKannaR