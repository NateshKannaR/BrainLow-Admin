services:
  - type: web
    name: brainlow-admin
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn admin_app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
    envVarGroups:
      - NateshKannaR